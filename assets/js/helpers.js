/*
// esto lo usaba dragscroll, no creo q lo necesitemos mas

function enableScrollOnDetail(status) {
    if(status) {
      document.querySelector('body').classList.remove('dragscroll')
      $detailContainer.classList.add('dragscroll')
    } else {
      document.querySelector('body').classList.add('dragscroll')
      $detailContainer.classList.remove('dragscroll')
    }

    dragscroll.reset()
  }
  */